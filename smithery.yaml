# Smithery deployment configuration for TCMB MCP
# https://smithery.ai/docs/cookbooks/python_custom_container

runtime: "container"

build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."

startCommand:
  type: "http"
